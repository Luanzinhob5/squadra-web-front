version: '3.8'

services:
  front:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./:/squadra-web-front
      - /squadra-web-front/node_modules
      
    command: sh -c "npm install && npm run dev -- --host"